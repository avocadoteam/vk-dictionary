export enum Skeys {
  appUser = 'appUser',
}

export enum AppUser {
  Yes = 'yes',
  No = 'no',
}

export enum FetchingStateName {
  Ads = 'ads',
  UserSKeys = 'user_storage_keys',
}

export enum FetchingStatus {
  Updating = 1,
  Ready,
  Error,
}
