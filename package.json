{
  "name": "vk-app",
  "version": "1.0",
  "private": true,
  "scripts": {
    "heroku-prebuild": "yarn build:server",
    "build:client": "cd client && yarn deploy",
    "build:server": "cd server && yarn && yarn build"
  }
}