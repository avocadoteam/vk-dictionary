export enum NameSpaces {
  ExampleSpace = '/exampleNamespace',
}

export enum SocketEvents {
  payment_complete = 'payment_complete',
}

export enum BusEvents {
  PAYMENT_COMPLETE = 'payment_complete',
}
